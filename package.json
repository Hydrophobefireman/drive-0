{
  "name": "drive",
  "version": "1.0.0",
  "description": "Drive monorepo",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently 'cd frontend && npm run start' 'cd worker && wrangler dev'",
    "prepare": "husky install"
  },
  "author": "Hydrophobefireman",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "husky": "^7.0.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.10.0",
    "concurrently": "^7.2.1",
    "typescript": "^4.6.4",
    "wrangler": "^2.0.5"
  }
}
